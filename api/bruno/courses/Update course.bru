meta {
  name: Update course
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/courses/{{courseUuid}}
  body: json
  auth: none
}

headers {
  Content-Type: application/merge-patch+json
  Accept: application/ld+json
}

body:json {
  {
    "title": "Title updated via Bruno"
  }
}

docs {
  Updates an existing course.
  Triggers a Varnish BAN → GET /courses and GET /courses/{uuid} switch to MISS.
}
